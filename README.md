# Meowchat-authorization-rpc

## Get started

**Start services**

```bash
go run authorization.go -f etc/authorization.yaml
```

Before starting the server, please replace the default config file in `etc` directory.
